# GUI - Jetson(ARM64)ではaptでインストール: sudo apt install python3-pyqt5
PyQt5>=5.15.0; platform_machine != 'aarch64'

# PyTorch - Jetson(ARM64)ではJetPack付属のPyTorchを使用
torch>=1.9.0; platform_machine != 'aarch64'
torchvision>=0.10.0; platform_machine != 'aarch64'

# NumPy/OpenCV/Matplotlib - Jetson(ARM64)ではaptでインストール:
# sudo apt install python3-numpy python3-opencv python3-matplotlib
numpy>=1.19.0; platform_machine != 'aarch64'
opencv-python>=4.5.0; platform_machine != 'aarch64'
matplotlib>=3.3.0; platform_machine != 'aarch64'

# 共通パッケージ
Pillow>=8.2.0
scikit-learn>=0.24.0
mlflow>=1.20.0
ultralytics>=8.0.0
tqdm>=4.62.0
timm>=1.0.14
grad-cam>=1.4.0
databricks-sdk>=0.20.0

# Google Colab連携
pydrive2>=1.19.0
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
PyYAML>=6.0
